{
  "name": "reversi",
  "version": "0.0.1",
  "description": "Computer reversi backed with webassembly",
  "main": "src/main.ts",
  "scripts": {
    "check:format": "prettier --check **/*.{js,ts,html,css}",
    "format": "prettier --write **/*.{js,ts,html,css}",
    "typecheck": "tsc --noEmit",
    "test": "echo \"INFO: no test specified\"",
    "build": "export REVERSI_VERSION=$(git describe --tag --always) && vite build",
    "start": "export REVERSI_VERSION=$(git describe --tag --always) && vite"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/oshikiri/reversi.git"
  },
  "author": "Takamasa Oshikiri <mail@oshikiri.org>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/oshikiri/reversi/issues"
  },
  "homepage": "https://github.com/oshikiri/reversi#readme",
  "engines": {
    "node": "^20"
  },
  "devDependencies": {
    "prettier": "3.8.1",
    "reversi-wasm": "file:../engine/pkg",
    "typescript": "^5.9.3",
    "vite": "7.3.1",
    "vite-plugin-wasm": "3.5.0"
  }
}
